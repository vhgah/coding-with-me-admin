export interface User {
    ip_address: string
    referral_url: string
    country: string
    city: string
    state: string
    is_robot: boolean
    user_agent: string
    device: string
    created_at: string
}